doctype html
html(lang='en')
  head
    title= "CS Experiment"
    meta(charset='utf-8')
    link(rel='stylesheet', href='/stylesheets/method.css')
    script(src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.0.0/jquery.js")
  body
    p(dir="rtl") אנא תאר/י את האסטרגיה בה השתמשת לבחירת הקודקודים:
    div(class="container")
      form(method="post" action="/experiment/method/add_method")
        input(dir="rtl" type="hidden" id="student_id" name="student_id" value=studentId)
        textarea(dir="rtl" type="text" placeholder="..." required id="method" name="method" style="height:200px")
        input(dir="rtl" type="submit" class="button" id="nextButton" value="Next")
